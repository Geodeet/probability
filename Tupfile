include_rules

# The rule to compile cpp files to object files
: foreach *.cpp |> !compile_files |>

# Linking the object files into an executable
: test.o fraction.o fraction.test.o |> !link_objects |> test
: main.o |> !link_objects |> prob
