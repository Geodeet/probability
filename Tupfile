include_rules

# The rule to compile cpp files to object files
: foreach *.cpp |> !cpp |>

# Linking the object files into an executable
: test.o | <fraction> <outcome> <util> |> $(COMPILER) $(LFLAGS) %f %<fraction> %<outcome> %<util> -o %o |> test